import styled from "styled-components";
import { BtnPointer } from "./mixins";

export const DarkThemeIcon = styled.svg`
  ${BtnPointer};
`;

export const LightThemeIcon = styled.svg`
  ${BtnPointer};
`;
